venv/
channel/node_modules/
homes/
.env
local.md
PLAN.md
relations.md
owner.md
agents/*
!agents/seed.md
!agents/seed/
!agents/seed/seed.md
agents/seed/avatar.*
!agents/*.example.md
!agents/*.example/
!agents/*.example/**
agents/.git/
/memory/
observatory/web/node_modules/
observatory/web/dist/

# geoloc — niche owner-personal location intake (Tasker→endpoint); removed from the
# PUBLIC tree but kept on the owner's disk so the running service survives (goal
# geoloc-remove). Name-anchored so a relocation can't silently un-ignore it.
bridges/geoloc.py
mcp/geoloc/
*.tsbuildinfo
**/routes*.json
!**/routes*.example.json
# per-agent provider/auth override (base_url + token_env name + model map); observatory-written.
# Real file is private (references .env token keys); the .example ships the schema for cloners.
runtime.json
!runtime.json.example
**/runners*.conf
!**/runners*.example.conf
units/
__pycache__/
media/
triggers/
sensors/
wacli-data/
# database backups — a pg_dump is the org's WHOLE state, full owner-PII; personal-tier,
# never pushed (asserted by privacy_gate.sh SURFACES). Offsite is an owner-decision.
backups/
# harness (goal 15) toxic-artifact tier: the probe corpus is live exploits and a
# success-transcript is a working exploit — never commit them, never let a real
# agent's context/RAG ingest them. Name-anchored so a relocation can't un-ignore.
harness/corpus/
harness/results/
**/*.probe.jsonl
**/*.toxic.*

# real-PII test fixtures — re-add only with certified-fake data (leak scrub 2026-07-27)
mcp/contacts/test_server.py
tier/
# career tier (owner-directed 2026-08-28) — the job-application funnel and every career
# application specific (employers/roles/comp) is human-personal tier per local.md. Already
# under the homes/ blanket; name-anchored here so a relocation or a homes/ negation can
# never silently un-ignore it. Meta-reference only (a career tier exists), never content.
**/career/
var/
