venv/
channel/node_modules/
homes/
.env
local.md
PLAN.md
relations.md
owner.md
agents/*
!agents/seed.md
!agents/seed/
!agents/seed/seed.md
agents/seed/avatar.*
!agents/*.example.md
!agents/*.example/
!agents/*.example/**
agents/.git/
memory/
observatory/web/node_modules/
observatory/web/dist/
*.tsbuildinfo
**/routes*.json
!**/routes*.example.json
**/runners*.conf
!**/runners*.example.conf
units/
__pycache__/
media/
triggers/
wacli-data/
# harness (goal 15) toxic-artifact tier: the probe corpus is live exploits and a
# success-transcript is a working exploit — never commit them, never let a real
# agent's context/RAG ingest them. Name-anchored so a relocation can't un-ignore.
harness/corpus/
harness/results/
**/*.probe.jsonl
**/*.toxic.*

# real-PII test fixtures — re-add only with certified-fake data (leak scrub 2026-07-27)
mcp/contacts/test_server.py
